#!/bin/bash

if [[ $EUID -ne 0 ]]; then
   echo "got root?" 1>&2
   exit 1
fi

/bin/mount -o remount,rw /
/bin/mount --bind /etc_org /etc
/bin/mount --bind /var_org /var
/bin/mount --bind /server_org /server
